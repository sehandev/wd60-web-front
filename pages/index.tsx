import React from "react";

import { Container, Landing } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Landing />
        </Container>
    );
};

export default Home;
